name: publish
on:
  workflow_dispatch:
    inputs:
      version:
        description: "Toolchain version (e.g. v1.0.0)"
        required: true
        default: "v1.0.0"
  push:
    tags:
      - "v*"

jobs:
  build:
    name: "build ${{ matrix.platform }}"
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        platform:
          - linux-amd64
          - linux-arm64
          - darwin-amd64
          - darwin-arm64
          - windows-amd64

    steps:
      - uses: actions/checkout@v4

      - name: Install deps
        run: |
          sudo apt-get update -q
          sudo apt-get install -y -q yq zstd unzip xz-utils

      - name: Determine version
        id: ver
        run: |
          if [[ "${{ github.event_name }}" == "push" ]]; then
            echo "version=${GITHUB_REF#refs/tags/}" >> "$GITHUB_OUTPUT"
          else
            echo "version=${{ inputs.version }}" >> "$GITHUB_OUTPUT"
          fi

      - name: Build toolchain tarball
        env:
          PLATFORMS: ${{ matrix.platform }}
        run: |
          chmod +x scripts/fetch-and-pack.sh
          scripts/fetch-and-pack.sh "${{ steps.ver.outputs.version }}"

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: toolchain-${{ matrix.platform }}
          path: |
            dist/toolchain-*.tar.zst
          retention-days: 1

  release:
    name: create release
    needs: build
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - uses: actions/checkout@v4

      - name: Determine version
        id: ver
        run: |
          if [[ "${{ github.event_name }}" == "push" ]]; then
            echo "version=${GITHUB_REF#refs/tags/}" >> "$GITHUB_OUTPUT"
          else
            echo "version=${{ inputs.version }}" >> "$GITHUB_OUTPUT"
          fi

      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: dist/
          merge-multiple: true

      - name: Generate manifest.json
        run: |
          VERSION="${{ steps.ver.outputs.version }}"
          entries=()
          for f in dist/toolchain-*.tar.zst; do
            sha=$(sha256sum "$f" | awk '{print $1}')
            plat=$(basename "$f" | sed "s/toolchain-${VERSION}-//;s/\.tar\.zst//")
            entries+=("{\"platform\":\"$plat\",\"file\":\"$(basename $f)\",\"sha256\":\"$sha\"}")
          done
          IFS=,
          echo "{\"toolchain_version\":\"$VERSION\",\"artifacts\":[${entries[*]}]}" | jq . > dist/manifest.json
          cat dist/manifest.json

      - name: Create GitHub release
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          VERSION="${{ steps.ver.outputs.version }}"
          gh release create "$VERSION" \
            --repo "${{ github.repository }}" \
            --title "Bundled toolchain $VERSION" \
            --notes "## Bundled Toolchain $VERSION

Platforms: linux-amd64, linux-arm64, darwin-amd64, darwin-arm64, windows-amd64

Tools bundled:
- uv 0.5.10
- node 22.11.0 (LTS) + npm + npx
- ripgrep 14.1.1
- jq 1.7.1" \
            dist/toolchain-*.tar.zst \
            dist/manifest.json \
            "manifests/$VERSION/manifest.yaml"
